---
name: weekly-report-statistics
description: 
    根据用户指令，完成物业收费系统的数据筛选、导出、清洗、统计工作。
    当用户要求：周例会报表、报表统计、周例会数据、收费数据统计时使用
---

## 核心流程

### Step1:自动化浏览器操作（**目标：登录系统并导出原始数据**）

- 1.**启动与导航**：打开Chrome浏览器，访问网址wuye-zy.ztstc.cn。
- 2.**登录逻辑**：检测登录页面的账号密码输入框。如果输入框中已存在数据（由浏览器自动填充），则直接点击登录按钮。如果未填充，请尝试触发浏览器的密码保存功能或提示我。
- 3.**菜单导航**：成功登录后，按顺序点击左侧导航菜单：首先点击“收费管理”，然后在出现的二级菜单中点击“费用生成”，接着在页面上找到并点击“常规费用一览表”进入查询界面。
- 4.**筛选条件**（**依次执行**，使用JS操作）：

    - 4.1 **组织机构**：
        - 点击"组织机构"下拉多选框展开。
        - 全选其中所有选项。
        - **点击页面空白处关闭下拉框**，确认选择已生效。
    - 4.2 **收费项目**：
        - **确认"组织机构"下拉框已关闭**
        - 点击"收费项目"下拉框展开
        - 仅勾选"物业费"这一项
        - **点击页面空白处关闭下拉框**
    - 4.3 **应收日期**：
        - **检查用户指令**是否包含具体日期（如"3月份"、"2026-03-01至2026-03-31"）
        - **若指令未指定日期**（默认逻辑）：
            - 开始日期：本年度1月1日（格式：YYYY-01-01）
            - 结束日期：本月最后一天（格式：YYYY-MM-DD）
        - **若指令指定了日期**：严格按照用户指定的日期范围设置
- 5.**执行与导出**：点击“搜索”按钮。待页面数据加载完成后，点击"导出"按钮，等待2分钟后，检查数据是否下载完成，如果完成则将数据保存至donwload/。

**异常处理**：
- 若登录失败：检查网络连接，重试一次
- 若页面加载超时：等待3秒后刷新重试

### Step2:数据处理

- 1.**文件识别**：监控下载文件夹，识别刚刚导出的最新文件，将其作为原始数据源。
- 2.**运行脚本**：将导出的原始文件路径作为参数传递给脚本，脚本路径为："scripts/statistics.py",
    - **注意：** 脚本需要接收命令行参数。
    ```bash
    # 统计数据脚本
    python scripts/statistics.py [导出的数据文件]
    ```
    
- 3.**生成报表**：脚本执行完成后，在当前位置生成最终的周例会报表。


### Step3:反馈
- 1.**反馈信息**：在任务完成后，请向我报告：导出的文件名、脚本执行状态（成功/失败）以及最终报表的保存位置。



